Then show the updated task list.
A file in .specs/current/ was modified.

Read all files in .specs/current/ and check consistency across:

- spec.md — scope, problem, acceptance criteria
- plan.md — approach, steps
- impact.md — affected areas, compatibility
- tests.md — required tests, covered invariants

Update only the files that are inconsistent with the change. Do not rewrite files unnecessarily.
Then sync the task list if plan.md was affected.
